namespace AT_RPG
{
    /// <summary>
    /// CSV에 저장되어있는 PlayerData의 순서를 보기쉽게 Enum으로 정리
    /// </summary>
    public enum PData
    {
        PlayerSlot,
        PlayerName,
        HealthPoint,
        StaminaPoint,
        AttackPoint,
        DefendPoint,
        Gold,
        HealPotion,
        MonsterPiece,
        Rock,
        Wood,
        Weapon
    }
}
